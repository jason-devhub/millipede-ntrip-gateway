#
# Authentification des sources locales (mountpoints poussés vers ce caster).
# En proxy pur, souvent vide ou commenté uniquement.
#
# Exemple :
# MON_MP:user:secret
#
# Ne commettez JAMAIS ce fichier rempli. Le `.gitignore` exclut `source.auth`.
